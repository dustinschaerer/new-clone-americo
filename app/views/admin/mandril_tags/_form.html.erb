<%= form_for([:admin, @mandril_tag]) do |f| %>
  <% if @mandril_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mandril_tag.errors.count, "error") %> prohibited this mandril_tag from being saved:</h2>

      <ul>
      <% @mandril_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :title %><br>
    <%= f.text_field :title %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
