<div class="page-header">
  <h1>Editing Inhouse Customer #<%= "#{@inhouse_customer.id}" %></h1>
</div>

<%= render 'form' %>

<%= link_to 'Show', [:admin, @inhouse_customer], class: 'btn btn-primary' %>
<%= link_to 'Back', admin_inhouse_customers_path, class: 'btn btn-danger' %>
