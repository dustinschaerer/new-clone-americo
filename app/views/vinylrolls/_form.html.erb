<%= form_for(@vinylroll) do |f| %>
  <% if @vinylroll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vinylroll.errors.count, "error") %> prohibited this vinylroll from being saved:</h2>

      <ul>
      <% @vinylroll.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :references %><br>
    <%= f.text_field :references %>
  </div>
  <div class="field">
    <%= f.label :references %><br>
    <%= f.text_field :references %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
